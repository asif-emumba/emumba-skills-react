# Attribution

## skills/react-best-practices

Mirrored from
[davila7/claude-code-templates](https://github.com/davila7/claude-code-templates),
path `cli-tool/components/skills/web-development/react-best-practices` — the
repository behind aitmpl.com.

The skill's own frontmatter declares `license: MIT` and
`author: Vercel Engineering`.

File contents are unmodified. The only change is location: it was flattened out
of the upstream `skills/<category>/<name>/` nesting into `skills/<name>/`, which
is the only layout Claude Code discovers.

Verify the upstream licence terms before including this skill in any client
deliverable.
