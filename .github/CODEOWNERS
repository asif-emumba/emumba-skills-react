# The frontend guild owns everything in this repo.
#
# In the previous shared-repo layout (testing-skills, alongside the backend
# plugin) this line could only ask for review; anyone with write access to the
# shared repo could still push here. Now the repo boundary is the permission
# boundary — grant write on THIS repo to the frontend guild only, and CODEOWNERS
# becomes enforcement rather than convention.
#
# Replace the placeholder with the real GitHub team before relying on it.
* @emumba/frontend-guild
